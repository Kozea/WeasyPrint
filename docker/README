Build image with:

    docker build -f docker/Dockerfile -t weasyprint .

(launch from root folder)
Run container with:

    weasyprint.bat <args>...

Latter script is a shortcut for command docker run.
weasyprint can access only files in "." folder, via docker volumes.

